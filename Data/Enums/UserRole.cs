namespace esquire
{
    /// <summary>
    /// This is not currently being used. The roles listed below are currently passed and stored as String values. 
    /// </summary>
    public enum UserRole
    {
        Admin = 1,
        Coordinator = 2,
        Coder = 3
    
    }
}
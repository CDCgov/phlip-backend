namespace esquire
{
    /// <summary>
    /// Flags are used for Users to provide feedback for a question.
    /// Green and Yellow flags are visible to User and Coordinator
    /// Red flags are visible to all Users
    /// </summary>
    public enum FlagType
    {
        Green = 1,
        Yellow = 2,
        Red = 3
    }
}